# Navigate to the backend directorycd blockchain/backend# Create virtual environment if it doesn't existpython -m venv venv# Activate virtual environment# On Windows:venv\Scripts\activate# On Linux/Mac:source venv/bin/activate# Install required packagespip install requests# Deactivate when done# On Windows:venv\Scripts\deactivate# On Linux/Mac:deactivate
# Create virtual environment if it doesn't exist
python -m venv venv
# Activate virtual environment# On Windows:
venv\Scripts\activate# On Linux/Mac:
source venv/bin/activate
# Install required packagespip install requests

# Deactivate when done







module.exports = {};
